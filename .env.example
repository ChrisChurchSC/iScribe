# logo.dev publishable token for EHR vendor logos in the Integrations section.
# Get a free token at https://www.logo.dev — copy this file to `.env` and fill it in.
# Without a token, the Integrations cards fall back to styled wordmark text.
VITE_LOGODEV_TOKEN=
